import styled from "styled-components";

export const LoginWrapper = styled.div`
  .user-info {
    width: 36px;
    height: 36px;

    img {
      width: inherit;
      height: inherit;
      border-radius: 50%;
    }
  }
`;
